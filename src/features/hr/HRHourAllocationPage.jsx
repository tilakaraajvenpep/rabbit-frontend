// HR Hour Allocation — reuse the same HourAllocationPage component from tenantadmin
// HR can access it via /hr/hours route, same UI, same backend endpoint (now allows HR role)
export { default } from '../tenantadmin/HourAllocationPage';
